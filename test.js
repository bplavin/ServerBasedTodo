/**
 * 1) put search bar on right side
 * 2) create parent component for filters. It should receive collection of unique user IDs.
 *      Unique user ids should be taken from state of app.js
 * 3) For each user id you need to render filter component (User id + checkbox)
 * 4) add user id to note box (so you can see who is the owner of note)
 *
 *
 *
 */
